openapi: "3.0.0"
info: { }

paths: { }

components:
  schemas:
    User:
      type: object
      required:
        - name
      properties:
        name:
          type: string
        registered:
          type: string
          format: date-time
