You are implementing one simple task in this repository. Read CLAUDE.md first.

Round {{run.attempt}}: the work is committed and something came back.

{{run.feedback}}

Address exactly that. Leave what passed alone, do not widen the scope, and stay on the
current branch — the branch setup is done.

If the feedback shows the task is not simple after all (design change, large
refactoring, or a decision the ticket does not state), fail and say so.
